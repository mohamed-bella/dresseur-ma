<div class="container justify-content-center">
     <!-- Button to Open the Modal -->
     <button type="button" class="btn btn-dark" data-bs-toggle="modal" data-bs-target="#filterModal">
          <i class="bi bi-funnel-fill"></i> Filtrer Les Résultats
     </button>
</div>

<!-- The Modal -->
<div class="modal fade" id="filterModal" tabindex="-1" aria-labelledby="filterModalLabel" aria-hidden="true">
     <div class="modal-dialog">
          <div class="modal-content">
               <div class="modal-header">
                    <h5 class="modal-title" id="filterModalLabel">Filtrer Les Résultats</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
               </div>
               <div class="modal-body">
                    <form method="GET" action="/announcements/filter">
                         <!-- Dog Breed Input with Autocomplete -->
                         <div class="mb-3">
                              <label for="breed" class="form-label" style="color: #000;">Race de chien</label>

                              <!-- Input for the dog breed with autocomplete -->
                              <input type="text" class="form-control" id="breedInput" name="title" placeholder="Entrez la race de chien" list="dogBreeds">

                              <!-- Datalist for predefined dog breeds -->
                              <datalist id="dogBreeds">
                                   <option value="Berger Allemand">
                                   <option value="Labrador Retriever">
                                   <option value="Golden Retriever">
                                   <option value="Bulldog Français">
                                   <option value="Bouledogue Anglais">
                                   <option value="Chihuahua">
                                   <option value="Rottweiler">
                                   <option value="Beagle">
                                   <option value="Poodle">
                                   <option value="Boxer">
                                   <option value="Cocker Spaniel">
                              </datalist>

                              <!-- Invalid feedback for validation -->
                              <div class="invalid-feedback">La race de chien est obligatoire.</div>
                         </div>

                         <!-- Filter by Price Range -->
                         <label for="minPrice">Prix Min:</label>
                         <input type="number" id="minPrice" name="minPrice" placeholder="Prix minimum" class="form-control mb-3">

                         <label for="maxPrice">Prix Max:</label>
                         <input type="number" id="maxPrice" name="maxPrice" placeholder="Prix maximum" class="form-control mb-3">

                         <!-- Location (Dropdown with Moroccan Cities) -->
                         <div class="mb-3">
                              <label for="location" class="form-label" style="color: #000;">Lieu</label>
                              <select class="form-select" id="location" name="location" required>
                                   <option value="Casablanca">Casablanca</option>
                                   <option value="Rabat">Rabat</option>
                                   <option value="Marrakech">Marrakech</option>
                                   <!-- Add more cities as needed -->
                              </select>
                              <div class="invalid-feedback">Le lieu est obligatoire.</div>
                         </div>

                         <!-- Filter Button inside Modal -->
                         <button type="submit" class="btn btn-dark"><i class="bi bi-funnel-fill"></i> Appliquer les filtres</button>
                    </form>
               </div>
          </div>
     </div>
</div>