<!-- Main Navigation Container -->
<div x-data="navbar" class="relative">
     <!-- Main Navbar -->
     <nav class="fixed top-0 inset-x-0 z-50 transition-all duration-500 bg-white border border-gray-200" :class="{ 'bg-white/95 backdrop-blur-md shadow-lg': isScrolled, 'bg-white': !isScrolled }">
          <div class="max-w-7xl mx-auto">
               <div class="px-4 sm:px-6">
                    <!-- Main Navigation Content -->
                    <div class="flex items-center justify-between h-20">
                         <!-- Left Section: Logo -->
                         <div class="flex items-center gap-6">
                              <!-- Mobile Menu Button -->
                              <button @click="toggleMobileMenu" class="lg:hidden relative group p-2 rounded-xl hover:bg-gray-100 active:bg-gray-200 transition-all duration-300">
                                   <div class="w-6 h-5 flex flex-col justify-between transform transition-transform duration-300" :class="{ 'rotate-180': isMobileMenuOpen }">
                                        <span class="w-full h-0.5 bg-gray-600 transform transition-transform duration-300" :class="{ 'rotate-45 translate-y-2': isMobileMenuOpen }"></span>
                                        <span class="w-full h-0.5 bg-gray-600 transition-opacity duration-300" :class="{ 'opacity-0': isMobileMenuOpen }"></span>
                                        <span class="w-full h-0.5 bg-gray-600 transform transition-transform duration-300" :class="{ '-rotate-45 -translate-y-2': isMobileMenuOpen }"></span>
                                   </div>
                              </button>

                              <!-- Logo -->
                              <a href="/" class="flex items-center gap-3 group">
                                   <div class="relative overflow-hidden rounded-xl">
                                        <img src="https://ndressilik.s3.eu-north-1.amazonaws.com/users/67338636f84bd237e37a39a6/gallery/1731497935188_ndressilikLogoTopNavbar.png" alt="NDRESSILIK" class="h-12 w-auto transition-transform duration-500 group-hover:scale-110">
                                   </div>
                              </a>
                         </div>

                         <!-- Center Section: Desktop Nav Links -->
                         <div class="hidden lg:flex items-center gap-1">
                              <% const navLinks=[ { href: '/services' , label: 'Services' , icon: 'fa-concierge-bell' }, { href: '/articles' , label: 'Articles' , icon: 'fa-newspaper' }, { href: '/professionnels' , label: 'Les professionnels' , icon: 'fa-award' }, { href: '/about' , label: 'À propos' , icon: 'fa-info-circle' }, { href: '/contact' , label: 'Contact' , icon: 'fa-envelope' } ]; %>

                                   <% navLinks.forEach(link=> { %>
                                        <a href="<%= link.href %>" class="group flex items-center gap-2 px-4 py-2 rounded-xl text-gray-600 font-medium
                                        hover:bg-gray-100 hover:text-gray-900 transition-all duration-300">
                                             <i class="fas <%= link.icon %> transform transition-transform duration-300 
                                             group-hover:scale-110"></i>
                                             <span>
                                                  <%= link.label %>
                                             </span>
                                        </a>
                                        <% }) %>
                         </div>

                         <!-- Right Section -->
                         <div class="flex items-center gap-2">
                              <!-- Search Button -->
                              <button @click="toggleSearch" class="p-2 rounded-xl hover:bg-gray-100 transition-all duration-300 group">
                                   <i class="fas fa-search text-lg text-gray-600 transition-transform duration-300
                                        group-hover:scale-110"></i>
                              </button>

                              <!-- Add Service Button -->
                              <% if (user) { %>
                                   <a href="/dashboard/new-service" class="group flex items-center gap-2 px-3 py-3 bg-gradient-to-r from-orange-500 to-orange-600
                                        text-white rounded-xl font-medium transition-all duration-300 
                                        hover:shadow-lg hover:shadow-orange-500/25 hover:-translate-y-0.5 active:translate-y-0">
                                        <i class="fas fa-plus transition-transform duration-500 group-hover:rotate-180"></i>
                                        <span class=" sm:inline text-sm">Votre Service</span>
                                   </a>
                                   <% } else { %>
                                        <button @click="openAuthModal" class="group flex items-center gap-2 px-3 py-3 bg-gradient-to-r from-orange-500 to-orange-600
                                        text-white rounded-xl font-medium transition-all duration-300 
                                        hover:shadow-lg hover:shadow-orange-500/25 hover:-translate-y-0.5 active:translate-y-0">
                                             <i class="fas fa-plus transition-transform duration-500 group-hover:rotate-180"></i>
                                             <span class=" sm:inline text-xs">Votre Service</span>
                                        </button>
                                        <% } %>

                                             <!-- User Profile -->
                                             <% if (user) { %>
                                                  <div class="relative" x-data="{ open: false }">
                                                       <button @click="open = !open" class="relative group p-1.5 rounded-full hover:bg-gray-100 transition-all duration-300">
                                                            <img src="<%= user.profileImage || '/default-avatar.png' %>" alt="Profile" class="w-8 h-8 rounded-full object-cover ring-2 ring-white 
                                                  transition-transform duration-300 group-hover:scale-105">
                                                       </button>

                                                       <!-- Dropdown Menu -->
                                                       <div x-show="open" @click.away="open = false" x-transition:enter="transition ease-out duration-200" x-transition:enter-start="opacity-0 scale-95" x-transition:enter-end="opacity-100 scale-100" x-transition:leave="transition ease-in duration-150" x-transition:leave-start="opacity-100 scale-100" x-transition:leave-end="opacity-0 scale-95" class="absolute right-0 mt-3 w-72 origin-top-right z-50">
                                                            <div class="bg-white rounded-2xl shadow-xl ring-1 ring-black/5 overflow-hidden">
                                                                 <!-- User Info -->
                                                                 <div class="p-4 bg-gray-50/50">
                                                                      <div class="flex items-center gap-3">
                                                                           <img src="<%= user.profileImage || '/default-avatar.png' %>" alt="Profile" class="w-12 h-12 rounded-full object-cover">
                                                                           <div class="flex-1 min-w-0">
                                                                                <div class="font-medium text-gray-900 truncate">
                                                                                     <%= user.displayName %>
                                                                                </div>
                                                                                <div class="text-sm text-gray-500 truncate">
                                                                                     <%= user.email %>
                                                                                </div>
                                                                           </div>
                                                                      </div>
                                                                 </div>

                                                                 <!-- Menu Items -->
                                                                 <div class="p-2">
                                                                      <a href="/dashboard" class="flex items-center gap-3 p-3 rounded-xl text-gray-700 
                                                            hover:bg-orange-50 transition-colors group">
                                                                           <div class="w-10 h-10 rounded-lg bg-orange-100 flex items-center justify-center
                                                                 transition-colors group-hover:bg-orange-200">
                                                                                <i class="fas fa-th-large text-orange-600"></i>
                                                                           </div>
                                                                           <div>
                                                                                <div class="font-medium">Dashboard</div>
                                                                                <div class="text-sm text-gray-500">Gérer vos services</div>
                                                                           </div>
                                                                      </a>

                                                                      <a href="/dashboard/profile" class="flex items-center gap-3 p-3 rounded-xl text-gray-700 
                                                            hover:bg-purple-50 transition-colors group">
                                                                           <div class="w-10 h-10 rounded-lg bg-purple-100 flex items-center justify-center
                                                                 transition-colors group-hover:bg-purple-200">
                                                                                <i class="fas fa-user text-purple-600"></i>
                                                                           </div>
                                                                           <div>
                                                                                <div class="font-medium">Profile</div>
                                                                                <div class="text-sm text-gray-500">Paramètres du compte</div>
                                                                           </div>
                                                                      </a>

                                                                      <a href="/logout" class="flex items-center gap-3 p-3 rounded-xl text-gray-700 
                                                            hover:bg-red-50 transition-colors group">
                                                                           <div class="w-10 h-10 rounded-lg bg-red-100 flex items-center justify-center
                                                                 transition-colors group-hover:bg-red-200">
                                                                                <i class="fas fa-sign-out-alt text-red-600"></i>
                                                                           </div>
                                                                           <div>
                                                                                <div class="font-medium">Déconnexion</div>
                                                                                <div class="text-sm text-gray-500">Quitter votre compte</div>
                                                                           </div>
                                                                      </a>
                                                                 </div>
                                                            </div>
                                                       </div>
                                                  </div>
                                                  <% } %>
                         </div>
                    </div>
               </div>
          </div>

          <!-- Mobile Menu -->
          <div x-show="isMobileMenuOpen" x-transition:enter="transition ease-out duration-200" x-transition:enter-start="opacity-0 -translate-y-4" x-transition:enter-end="opacity-100 translate-y-0" x-transition:leave="transition ease-in duration-150" x-transition:leave-start="opacity-100 translate-y-0" x-transition:leave-end="opacity-0 -translate-y-4" class="lg:hidden border-t border-gray-100">
               <div class="max-h-[calc(100vh-5rem)] overflow-y-auto">
                    <div class="grid grid-cols-2 gap-2 p-4">
                         <% navLinks.forEach(link=> { %>
                              <a href="<%= link.href %>" class="flex flex-col items-center gap-2 p-4 rounded-xl text-gray-600 
                                   hover:bg-gray-50 hover:text-gray-900 transition-all duration-300 group">
                                   <i class="fas <%= link.icon %> text-xl transform transition-transform duration-300 
                                        group-hover:scale-110"></i>
                                   <span class="text-sm font-medium">
                                        <%= link.label %>
                                   </span>
                              </a>
                              <% }) %>
                    </div>

                    <!-- Add Service Button for Mobile -->
                    <div class="p-4 border-t border-gray-100">
                         <% if (user) { %>
                              <a href="/dashboard/new-service" class="w-full flex items-center justify-center gap-2 p-3 bg-gradient-to-r 
                                   from-orange-500 to-orange-600 text-white rounded-xl font-medium 
                                   transition-all duration-300">
                                   <i class="fas fa-plus"></i>
                                   <span>Votre Service</span>
                              </a>
                              <% } else { %>
                                   <button @click="openAuthModal" class="w-full flex items-center justify-center gap-2 p-3 bg-gradient-to-r 
                                   from-orange-500 to-orange-600 text-white rounded-xl font-medium 
                                   transition-all duration-300">
                                        <i class="fas fa-plus"></i>
                                        <span>Votre Service</span>
                                   </button>
                                   <% } %>
                    </div>

                    <% if (user) { %>
                         <div class="border-t border-gray-100 p-4">
                              <div class="flex items-center gap-4 p-4 bg-gray-50 rounded-xl mb-4">
                                   <img src="<%= user.profileImage || '/default-avatar.png' %>" alt="Profile" class="w-12 h-12 rounded-full object-cover">
                                   <div>
                                        <div class="font-medium text-gray-900">
                                             <%= user.displayName %>
                                        </div>
                                        <div class="text-sm text-gray-500">
                                             <%= user.email %>
                                        </div>
                                   </div>
                              </div>

                              <div class="grid grid-cols-2 gap-2">
                                   <a href="/dashboard" class="flex items-center justify-center gap-2 p-3 rounded-xl text-gray-600 
                                        hover:bg-gray-50 transition-all duration-300 group">
                                        <i class="fas fa-th-large"></i>
                                        <span>Dashboard</span>
                                   </a>
                                   <a href="/dashboard/profile" class="flex items-center justify-center gap-2 p-3 rounded-xl text-gray-600 
                                        hover:bg-gray-50 transition-all duration-300 group">
                                        <i class="fas fa-user"></i>
                                        <span>Profile</span>
                                   </a>
                                   <a href="/logout" class="flex items-center justify-center gap-2 p-3 rounded-xl text-gray-600 
                                        hover:bg-gray-50 transition-all duration-300 group">
                                        <i class="fas fa-sign-out-alt"></i>
                                        <span>Déconnexion</span>
                                   </a>
                              </div>
                         </div>
                         <% } else { %>
                              <div class="p-4 border-t border-gray-100">
                                   <!-- <button @click="openAuthModal" class="w-full flex items-center justify-center gap-2 p-3 bg-gradient-to-r 
                                   from-orange-500 to-orange-600 text-white rounded-xl font-medium 
                                   transition-all duration-300">
                                        <i class="fas fa-sign-in-alt"></i>
                                        <span>Commencer</span>
                                   </button> -->
                              </div>
                              <% } %>
               </div>
          </div>
     </nav>

     <!-- Search Overlay -->
     <div x-show="isSearchOpen" x-cloak @keydown.escape="isSearchOpen = false" class="fixed inset-0 z-50 flex items-start justify-center p-4 bg-black/50 backdrop-blur-sm" x-transition:enter="transition ease-out duration-200" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-150" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
          <div class="w-full max-w-2xl pt-20">
               <div class="bg-white rounded-2xl shadow-xl overflow-hidden">
                    <div class="relative">
                         <input type="text" placeholder="Rechercher un service..." class="w-full h-16 pl-16 pr-14 text-lg bg-white border-none focus:ring-0" x-model="searchQuery" @keydown.enter.prevent="performSearch" x-ref="searchInput">
                         <i class="fas fa-search absolute left-5 top-1/2 -translate-y-1/2 text-xl text-gray-400"></i>
                         <button @click="isSearchOpen = false" class="absolute right-4 top-1/2 -translate-y-1/2 p-2 rounded-lg 
                              hover:bg-gray-100 transition-colors duration-300">
                              <i class="fas fa-times text-lg text-gray-500"></i>
                         </button>
                    </div>
                    <div class="p-4 border-t border-gray-100">
                         <h3 class="text-sm font-medium text-gray-500 mb-3">Recherches populaires</h3>
                         <div class="flex flex-wrap gap-2">
                              <button @click="searchQuery = 'dressage'; performSearch()" class="px-4 py-2 bg-gray-50 hover:bg-gray-100 rounded-lg text-gray-700 
                                   text-sm transition-colors duration-300">
                                   <i class="fas fa-dog mr-2 text-gray-400"></i>
                                   Dressage
                              </button>
                              <button @click="searchQuery = 'toilettage'; performSearch()" class="px-4 py-2 bg-gray-50 hover:bg-gray-100 rounded-lg text-gray-700 
                                   text-sm transition-colors duration-300">
                                   <i class="fas fa-cut mr-2 text-gray-400"></i>
                                   Toilettage
                              </button>
                              <button @click="searchQuery = 'pension'; performSearch()" class="px-4 py-2 bg-gray-50 hover:bg-gray-100 rounded-lg text-gray-700 
                                   text-sm transition-colors duration-300">
                                   <i class="fas fa-home mr-2 text-gray-400"></i>
                                   Pension
                              </button>
                         </div>
                    </div>
               </div>
          </div>
     </div>

     <!-- Auth Modal -->
     <div x-show="isAuthModalOpen" x-cloak @keydown.escape="closeAuthModal" class="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/50 backdrop-blur-sm" :class="{ 'pointer-events-none': !isAuthModalOpen }">
          <div class="w-full max-w-md transform transition-all" x-show="isAuthModalOpen" x-trap.noscroll="isAuthModalOpen" @click.away="closeAuthModal" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0 scale-95" x-transition:enter-end="opacity-100 scale-100" x-transition:leave="transition ease-in duration-200" x-transition:leave-start="opacity-100 scale-100" x-transition:leave-end="opacity-0 scale-95">
               <div class="bg-white rounded-2xl shadow-xl overflow-hidden">
                    <!-- Modal Header -->
                    <div class="relative h-32 bg-gradient-to-br from-orange-400 to-orange-600
                         flex items-center justify-center">
                         <div class="absolute inset-0 bg-black/10"></div>
                         <img src="https://ndressilik.s3.eu-north-1.amazonaws.com/users/67338636f84bd237e37a39a6/gallery/1731497935188_ndressilikLogoTopNavbar.png" alt="NDRESSILIK" class="h-12 relative z-10">
                         <button @click="closeAuthModal" type="button" class="absolute top-4 right-4 p-2 rounded-lg 
                              bg-black/10 hover:bg-black/20 text-white
                              transition-colors duration-300">
                              <i class="fas fa-times"></i>
                         </button>
                    </div>
                    <div class="p-6">
                         <div class="text-center mb-6">
                              <h2 class="text-2xl font-bold text-gray-900">Bienvenue</h2>
                              <p class="mt-2 text-gray-600">
                                   Connectez-vous pour accéder à toutes les fonctionnalités
                              </p>
                         </div>
                         <a href="/auth/google" class="group flex items-center justify-center gap-3 w-full p-4
                              border-2 border-gray-200 rounded-xl
                              hover:border-orange-500 hover:bg-orange-50
                              transition-all duration-300">
                              <i class="fab fa-google text-xl text-gray-700
                                   group-hover:text-orange-500 transition-colors"></i>
                              <span class="font-medium text-gray-700
                                   group-hover:text-orange-500 transition-colors">
                                   Continuer avec Google
                              </span>
                         </a>
                         <div class="mt-8 grid grid-cols-2 gap-4">
                              <div class="text-center p-4 rounded-xl bg-gray-50">
                                   <i class="fas fa-shield-alt text-2xl text-orange-500 mb-2"></i>
                                   <h3 class="font-medium text-gray-900">Sécurisé</h3>
                                   <p class="text-sm text-gray-500">
                                        Connexion sécurisée via Google
                                   </p>
                              </div>
                              <div class="text-center p-4 rounded-xl bg-gray-50">
                                   <i class="fas fa-bolt text-2xl text-orange-500 mb-2"></i>
                                   <h3 class="font-medium text-gray-900">Rapide</h3>
                                   <p class="text-sm text-gray-500">
                                        Commencez en quelques secondes
                                   </p>
                              </div>
                         </div>
                    </div>
               </div>
          </div>
     </div>
</div>

<script>
     document.addEventListener('alpine:init', () => {
          Alpine.data('navbar', () => ({
               isScrolled: false,
               isMobileMenuOpen: false,
               isSearchOpen: false,
               isAuthModalOpen: false,
               searchQuery: '',

               init() {
                    this.isAuthModalOpen = window.location.search.includes('auth=true');
                    window.history.replaceState({}, '', window.location.pathname);

                    window.addEventListener('scroll', () => {
                         this.isScrolled = window.pageYOffset > 20;
                    });

                    this.$watch('isMobileMenuOpen', value => {
                         this.toggleScrollLock(value);
                    });

                    this.$watch('isSearchOpen', value => {
                         this.toggleScrollLock(value);
                         if (value) {
                              this.$nextTick(() => this.$refs.searchInput?.focus());
                         }
                    });

                    this.$watch('isAuthModalOpen', value => {
                         this.toggleScrollLock(value);
                    });
               },

               toggleMobileMenu() {
                    this.isMobileMenuOpen = !this.isMobileMenuOpen;
                    if (this.isMobileMenuOpen) {
                         this.isSearchOpen = false;
                         this.isAuthModalOpen = false;
                    }
               },

               toggleSearch() {
                    this.isSearchOpen = !this.isSearchOpen;
                    if (this.isSearchOpen) {
                         this.isMobileMenuOpen = false;
                         this.isAuthModalOpen = false;
                    }
               },

               openAuthModal() {
                    this.isAuthModalOpen = true;
                    this.isMobileMenuOpen = false;
                    this.isSearchOpen = false;
               },

               closeAuthModal() {
                    this.isAuthModalOpen = false;
               },

               toggleScrollLock(shouldLock) {
                    if (shouldLock) {
                         document.body.style.overflow = 'hidden';
                         document.body.style.paddingRight = `${this.getScrollbarWidth()}px`;
                    } else {
                         document.body.style.overflow = '';
                         document.body.style.paddingRight = '';
                    }
               },

               getScrollbarWidth() {
                    return window.innerWidth - document.documentElement.clientWidth;
               },

               async performSearch() {
                    if (!this.searchQuery.trim()) return;

                    try {
                         window.location.href = `/search?q=${encodeURIComponent(this.searchQuery.trim())}`;
                    } catch (error) {
                         console.error('Search error:', error);
                    }
               }
          }));
     });
</script>

<style>
     [x-cloak] {
          display: none !important;
     }

     .modal-backdrop-enter-active,
     .modal-backdrop-leave-active {
          transition: opacity 0.3s ease;
     }

     .modal-backdrop-enter-from,
     .modal-backdrop-leave-to {
          opacity: 0;
     }

     .modal-enter-active,
     .modal-leave-active {
          transition: all 0.3s ease;
     }

     .modal-enter-from,
     .modal-leave-to {
          opacity: 0;
          transform: scale(0.95);
     }

     @keyframes slideInDown {
          from {
               transform: translateY(-100%);
               opacity: 0;
          }

          to {
               transform: translateY(0);
               opacity: 1;
          }
     }

     @keyframes fadeScale {
          from {
               transform: scale(0.95);
               opacity: 0;
          }

          to {
               transform: scale(1);
               opacity: 1;
          }
     }
</style>
<div class="mt-20"></div>