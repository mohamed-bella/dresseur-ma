<!-- Hero Section -->
<div class="relative min-h-screen bg-[#f8f5f0] text-gray-800 overflow-hidden">
     <!-- Background Pattern -->


     <!-- Main Content -->
     <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative">
          <div class="pt-20 pb-16 text-center">
               <!-- Professional Title Section -->
               <div class="max-w-4xl mx-auto mb-12">
                    <h1 class="font-serif text-6xl md:text-8xl font-bold mb-8 text-gray-800 animate-fade-in">
                         Vous êtes un
                         <span class="block mt-4 text-5xl md:text-7xl">
                              Dresseur, Vétérinaire, Toiletteur ?
                         </span>
                    </h1>
                    <p class="font-serif text-2xl text-gray-600 mb-12 animate-fade-in-up leading-relaxed">
                         Rejoignez notre communauté de professionnels et développez votre activité en ligne.
                         <span class="block mt-2">Gagnez en visibilité et trouvez de nouveaux clients.</span>
                    </p>

                    <!-- Search Bar -->
                    <form class="max-w-2xl mx-auto relative mb-12 animate-fade-in-up" action="/search" method="get">
                         <div class="relative flex items-center">
                              <input name="q" type="search" placeholder="Rechercher des services..." class="w-full pl-14 pr-16 py-6 rounded-xl border-2 border-gray-300 bg-white text-gray-800 placeholder-gray-500 focus:border-gray-400 focus:ring-4 focus:ring-gray-200 outline-none text-lg transition-all duration-300 shadow-sm hover:shadow-md font-serif" autocomplete="off">
                              <div class="absolute left-5 top-1/2 -translate-y-1/2">
                                   <i class="fas fa-search text-2xl text-gray-400"></i>
                              </div>
                              <button type="submit" class="absolute right-3 top-1/2 -translate-y-1/2 bg-gray-800 text-white px-3 py-3 rounded-xl font-serif hover:bg-gray-700 transform hover:-translate-y-0.5 transition-all duration-300 flex items-center gap-2">
                                   <span>Rechercher</span>
                                   <i class="fas fa-arrow-right"></i>
                              </button>
                         </div>
                    </form>

                    <!-- Add Service Button -->
                    <button onclick="document.getElementById('loginModal').classList.remove('hidden')" class="bg-gray-800 text-white px-8 py-4 rounded-xl font-serif text-lg hover:bg-gray-700 transform hover:-translate-y-0.5 transition-all duration-300 shadow-lg flex items-center gap-3 mx-auto">
                         <i class="fas fa-plus-circle text-2xl"></i>
                         <span>Ajouter un nouveau service</span>
                    </button>
               </div>
          </div>
     </div>

     <!-- Login Modal -->
     <div id="loginModal" class="hidden fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
          <div class="bg-[#f8f5f0] text-gray-800 p-8 rounded-xl max-w-md w-full mx-4 shadow-2xl">
               <div class="text-center">
                    <h2 class="font-serif text-3xl font-bold mb-6">Connexion requise</h2>
                    <p class="text-gray-600 mb-8 font-serif">Connectez-vous pour ajouter un nouveau service</p>

                    <button onclick="window.location.href='/auth/google'" class="w-full bg-white border-2 border-gray-800 text-gray-800 px-6 py-4 rounded-xl font-serif hover:bg-gray-100 transition-all duration-300 flex items-center justify-center gap-3 mb-6 shadow-md">
                         <i class="fab fa-google text-2xl"></i>
                         <span>Continuer avec Google</span>
                    </button>

                    <button onclick="document.getElementById('loginModal').classList.add('hidden')" class="font-serif text-gray-600 hover:text-gray-800 transition-colors duration-300">
                         Annuler
                    </button>
               </div>
          </div>
     </div>
</div>

<style>
     @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&display=swap');

     .font-serif {
          font-family: 'Playfair Display', serif;
     }

     .animate-fade-in {
          animation: fadeIn 0.8s ease-out forwards;
     }

     .animate-fade-in-up {
          animation: fadeInUp 0.8s ease-out forwards;
     }

     @keyframes fadeIn {
          from {
               opacity: 0;
          }

          to {
               opacity: 1;
          }
     }

     @keyframes fadeInUp {
          from {
               opacity: 0;
               transform: translateY(20px);
          }

          to {
               opacity: 1;
               transform: translateY(0);
          }
     }
</style>

<!-- Add to your existing scripts -->
<script>
     // Close modal when clicking outside
     document.getElementById('loginModal').addEventListener('click', function (e) {
          if (e.target === this) {
               this.classList.add('hidden');
          }
     });
</script>
<style>
     @keyframes float-1 {

          0%,
          100% {
               transform: translate(0, 0) rotate(0deg);
          }

          25% {
               transform: translate(10px, -10px) rotate(5deg);
          }

          50% {
               transform: translate(20px, 0) rotate(0deg);
          }

          75% {
               transform: translate(10px, 10px) rotate(-5deg);
          }
     }

     .animate-float-1 {
          animation: float-1 8s infinite ease-in-out;
     }

     .animate-fade-in {
          animation: fadeIn 1s ease-out;
     }

     .animate-fade-in-up {
          animation: fadeInUp 1s ease-out;
     }

     @keyframes fadeIn {
          from {
               opacity: 0;
          }

          to {
               opacity: 1;
          }
     }

     @keyframes fadeInUp {
          from {
               opacity: 0;
               transform: translateY(20px);
          }

          to {
               opacity: 1;
               transform: translateY(0);
          }
     }
</style>

<!-- Slider des Services -->
<div class="py-16 bg-gray-50 overflow-hidden px-2">
     <!-- Section Header -->
     <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mb-12 text-center">
          <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">Nos Services Animaliers</h2>
          <p class="text-xl text-gray-600 max-w-2xl mx-auto">Découvrez notre gamme complète de services professionnels pour prendre soin de votre compagnon à quatre pattes</p>
     </div>
     <!-- Conteneur du Slider -->
     <div class="overflow-x-auto hide-scrollbar -mx-4">
          <div class="flex space-x-4 md:space-x-6 p-4 transition-transform duration-300">
               <% const serviceCards=[ { icon: '🐕' , title: 'Dressage' , description: 'Éducation professionnelle pour chiens' , bgColor: 'bg-emerald-50' , hoverColor: 'group-hover:bg-emerald-100' , iconBg: 'bg-emerald-100' }, { icon: '✂️' , title: 'Toilettage' , description: 'Services complets de toilettage' , bgColor: 'bg-sky-50' , hoverColor: 'group-hover:bg-sky-100' , iconBg: 'bg-sky-100' }, { icon: '🏠' , title: 'Pension' , description: 'Hébergement de luxe' , bgColor: 'bg-purple-50' , hoverColor: 'group-hover:bg-purple-100' , iconBg: 'bg-purple-100' }, { icon: '🦮' , title: 'Promenade' , description: 'Promenade professionnelle pour chiens' , bgColor: 'bg-amber-50' , hoverColor: 'group-hover:bg-amber-100' , iconBg: 'bg-amber-100' }, { icon: '🐾' , title: 'Vétérinaire' , description: 'Soins vétérinaires spécialisés' , bgColor: 'bg-pink-50' , hoverColor: 'group-hover:bg-pink-100' , iconBg: 'bg-pink-100' }, { icon: '🚚' , title: 'Transport' , description: 'Service de transport sécurisé pour chiens' , bgColor: 'bg-teal-50' , hoverColor: 'group-hover:bg-teal-100' , iconBg: 'bg-teal-100' } ] %>

                    <% serviceCards.forEach(card=> { %>
                         <a href="/services/<%= card.title.toLowerCase() %>" class="flex-none w-[280px] md:w-[320px] group">
                              <div class="<%= card.bgColor %> <%= card.hoverColor %> rounded-3xl p-6 md:p-8 shadow-sm hover:shadow-xl transition-all duration-300 h-full">
                                   <!-- Icône avec fond -->
                                   <div class="<%= card.iconBg %> w-14 h-14 rounded-2xl flex items-center justify-center mb-4">
                                        <span class="text-3xl">
                                             <%= card.icon %>
                                        </span>
                                   </div>

                                   <!-- Contenu -->
                                   <h3 class="text-xl md:text-2xl font-bold mb-2 group-hover:text-blue-600 transition-colors">
                                        <%= card.title %>
                                   </h3>

                                   <p class="text-sm text-gray-600 mb-6">
                                        <%= card.description %>
                                   </p>

                                   <!-- Flèche -->
                                   <div class="flex items-center text-blue-600 text-sm font-medium group-hover:translate-x-2 transition-transform">
                                        <span>Explorer</span>
                                        <i class="fas fa-arrow-right ml-2"></i>
                                   </div>
                              </div>
                         </a>
                         <% }) %>
          </div>
     </div>
     <!-- Décoration Patte -->
     <div class="absolute -bottom-12 left-1/2 transform -translate-x-1/2 text-gray-200 opacity-20">
          <svg class="w-24 h-24" viewBox="0 0 512 512">
               <path fill="currentColor" d="M256 224c-79.5 0-144 64.5-144 144s64.5 144 144 144 144-64.5 144-144-64.5-144-144-144zm0 240c-52.9 0-96-43.1-96-96s43.1-96 96-96 96 43.1 96 96-43.1 96-96 96zm0-128c-17.7 0-32 14.3-32 32s14.3 32 32 32 32-14.3 32-32-14.3-32-32-32z" />
          </svg>
     </div>
</div>
</div>
</div>

<style>
     /* Cacher la barre de défilement mais conserver la fonctionnalité */
     .hide-scrollbar {
          -ms-overflow-style: none;
          scrollbar-width: none;
     }

     .hide-scrollbar::-webkit-scrollbar {
          display: none;
     }

     /* Activer le défilement fluide */
     .overflow-x-auto {
          -webkit-overflow-scrolling: touch;
          scroll-behavior: smooth;
     }

     /* Effets de survol des cartes */
     .group:hover {
          transform: translateY(-4px);
     }
</style>

<script>
     // Facultatif : ajouter la fonctionnalité de glissement au toucher
     document.addEventListener('DOMContentLoaded', function () {
          const slider = document.querySelector('.overflow-x-auto');
          let isDown = false;
          let startX;
          let scrollLeft;

          slider.addEventListener('mousedown', (e) => {
               isDown = true;
               startX = e.pageX - slider.offsetLeft;
               scrollLeft = slider.scrollLeft;
          });

          slider.addEventListener('mouseleave', () => {
               isDown = false;
          });

          slider.addEventListener('mouseup', () => {
               isDown = false;
          });

          slider.addEventListener('mousemove', (e) => {
               if (!isDown) return;
               e.preventDefault();
               const x = e.pageX - slider.offsetLeft;
               const walk = (x - startX) * 2;
               slider.scrollLeft = scrollLeft - walk;
          });
     });
</script>