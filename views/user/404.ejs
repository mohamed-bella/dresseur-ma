<!DOCTYPE html>
<html lang="fr" class="h-full">

<head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Page Non Trouvée - Erreur 404 - Ndressilik</title>
     <!-- Fonts -->
     <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
     <!-- Tailwind CSS -->
     <script src="https://cdn.tailwindcss.com"></script>
     <!-- Alpine.js -->
     <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>

     <script>
          tailwind.config = {
               darkMode: 'class',
               theme: {
                    extend: {
                         fontFamily: {
                              sans: ['Inter', 'sans-serif'],
                         },
                    }
               }
          }
     </script>
</head>

<body class="h-full bg-gradient-to-tr from-blue-50 via-white to-purple-50 dark:from-gray-900 dark:via-gray-900 dark:to-gray-800 font-sans antialiased">
     <div class="min-h-full px-6 py-24 flex flex-col items-center justify-center relative">
          <!-- Main Container -->
          <div class="w-full max-w-lg mx-auto text-center">
               <!-- 404 SVG Illustration -->
               <div class="mb-8" x-data="{ hover: false }">
                    <svg @mouseenter="hover = true" @mouseleave="hover = false" class="w-48 h-48 mx-auto transition-transform duration-300" :class="{ 'scale-110': hover }" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                         <path d="M12 8L16 12L12 16M8 12H16M12 22C17.5228 22 22 17.5228 22 12C22 6.47715 17.5228 2 12 2C6.47715 2 2 6.47715 2 12C2 17.5228 6.47715 22 12 22Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-blue-500 dark:text-blue-400" />
                    </svg>
               </div>

               <!-- Content -->
               <div class="space-y-6 bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm p-8 rounded-2xl shadow-lg">
                    <div class="space-y-2">
                         <h1 class="text-3xl font-bold text-gray-900 dark:text-white">
                              Page Non Trouvée
                         </h1>
                         <p class="text-lg font-medium text-blue-600 dark:text-blue-400">
                              Erreur 404
                         </p>
                    </div>

                    <p class="text-base text-gray-600 dark:text-gray-300 max-w-md mx-auto">
                         La page que vous recherchez semble avoir disparu. Elle a peut-être été déplacée ou supprimée.
                    </p>

                    <!-- Interactive Element -->
                    <div class="pt-6" x-data="{ attempts: 0 }">
                         <button @click="attempts++" class="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-600 transition-colors duration-200">
                              <span x-text="attempts === 0 ? 'Chercher la page' : `Tentative ${attempts}`"></span>
                              <span x-show="attempts > 0" class="text-sm">🔍</span>
                         </button>

                         <!-- Fun Messages -->
                         <div x-show="attempts > 0" x-transition.duration.300ms class="mt-4 text-sm text-gray-600 dark:text-gray-400">
                              <template x-if="attempts === 1">
                                   <p>Hmm, pas ici...</p>
                              </template>
                              <template x-if="attempts === 2">
                                   <p>On continue à chercher ?</p>
                              </template>
                              <template x-if="attempts >= 3">
                                   <p>Peut-être qu'on devrait retourner à l'accueil 😅</p>
                              </template>
                         </div>
                    </div>

                    <!-- Actions -->
                    <div class="pt-6 flex flex-col sm:flex-row gap-4 justify-center">
                         <a href="/" class="inline-flex items-center justify-center px-5 py-2.5 rounded-lg bg-blue-600 text-white hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors duration-200">
                              Retourner à l'accueil
                         </a>
                         <button onclick="history.back()" class="inline-flex items-center justify-center px-5 py-2.5 rounded-lg bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2 transition-colors duration-200">
                              Page précédente
                         </button>
                    </div>
               </div>

               <!-- Footer -->
               <p class="mt-8 text-sm text-gray-500 dark:text-gray-400">
                    Si vous pensez qu'il s'agit d'une erreur,
                    <a href="/contact" class="text-blue-600 dark:text-blue-400 hover:underline">
                         contactez-nous
                    </a>
               </p>
          </div>
     </div>

     <style>
          /* Optional: Smooth Scrolling */
          html {
               scroll-behavior: smooth;
          }

          /* Optional: Better Focus Outline */
          *:focus-visible {
               outline: 2px solid currentColor;
               outline-offset: 2px;
          }

          /* Optional: Remove Focus Outline for Mouse Users */
          *:focus:not(:focus-visible) {
               outline: none;
          }

          @media (prefers-reduced-motion: reduce) {
               * {
                    animation-duration: 0.01ms !important;
                    animation-iteration-count: 1 !important;
                    transition-duration: 0.01ms !important;
                    scroll-behavior: auto !important;
               }
          }
     </style>
</body>

</html>