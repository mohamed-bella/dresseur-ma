<!DOCTYPE html>
<html lang="fr" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page Non Trouvée - Erreur 404 - Ndressilik</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>

    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                    },
                    animation: {
                        'float': 'float 6s ease-in-out infinite',
                        'spin-slow': 'spin 8s linear infinite',
                    },
                    keyframes: {
                        float: {
                            '0%, 100%': { transform: 'translateY(0)' },
                            '50%': { transform: 'translateY(-20px)' },
                        }
                    }
                }
            }
        }
    </script>
</head>

<body class="h-full bg-gradient-to-tr from-blue-50 via-white to-purple-50 dark:from-gray-900 dark:via-gray-900 dark:to-gray-800 font-sans antialiased">
    <!-- Animated Background Elements -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
        <div class="absolute inset-0" id="particles"></div>
    </div>

    <div class="min-h-full px-6 py-24 flex flex-col items-center justify-center relative">
        <!-- Main Container -->
        <div class="w-full max-w-lg mx-auto text-center">
            <!-- 404 Animated Scene -->
            <div class="mb-12 relative" x-data="{ hover: false }">
                <div class="animate-float">
                    <!-- Astronaut illustration -->
                    <svg class="w-64 h-64 mx-auto" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path class="text-blue-500 dark:text-blue-400" d="M12 2C6.47715 2 2 6.47715 2 12C2 17.5228 6.47715 22 12 22C17.5228 22 22 17.5228 22 12C22 6.47715 17.5228 2 12 2Z" stroke="currentColor" stroke-width="2"/>
                        <path class="text-blue-600 dark:text-blue-300" d="M8 12L16 12M12 8L12 16" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                        <!-- Add more paths for detailed astronaut illustration -->
                    </svg>
                    
                    <!-- Floating Elements -->
                    <div class="absolute top-0 left-1/2 transform -translate-x-1/2 w-full h-full">
                        <div class="absolute top-0 left-0 w-4 h-4 bg-purple-400/30 rounded-full animate-spin-slow"></div>
                        <div class="absolute top-1/4 right-0 w-6 h-6 bg-blue-400/30 rounded-full animate-float"></div>
                        <div class="absolute bottom-0 left-1/4 w-5 h-5 bg-pink-400/30 rounded-full animate-bounce"></div>
                    </div>
                </div>
            </div>

            <!-- Content Card -->
            <div class="space-y-8 bg-white/90 dark:bg-gray-800/90 backdrop-blur-lg p-8 rounded-2xl shadow-lg transform transition-all duration-500 hover:scale-105">
                <!-- Header -->
                <div class="space-y-3">
                    <h1 class="text-4xl font-bold text-gray-900 dark:text-white tracking-tight">
                        404
                    </h1>
                    <p class="text-xl font-medium bg-gradient-to-r from-blue-600 to-purple-600 dark:from-blue-400 dark:to-purple-400 bg-clip-text text-transparent">
                        Page Non Trouvée
                    </p>
                </div>

                <!-- Message -->
                <p class="text-base text-gray-600 dark:text-gray-300 max-w-md mx-auto leading-relaxed">
                    Oups ! On dirait que vous vous êtes aventuré dans l'espace inconnu. 
                    Cette page semble avoir disparu dans un trou noir. 🚀
                </p>

                <!-- Interactive Search Element -->
                <div class="pt-6" x-data="{ searchCount: 0, messages: [
                    'Scanning de l\'univers... 🔭',
                    'Cherchons dans une autre galaxie... 🌌',
                    'Houston, nous avons un problème! 🛸',
                    'Temps de rentrer à la base... 🏠'
                ] }">
                    <button @click="searchCount++" 
                            class="group relative px-6 py-3 rounded-lg bg-gradient-to-r from-blue-500 to-purple-500 text-white overflow-hidden transition-all duration-300 hover:shadow-lg">
                        <span class="relative z-10" x-text="searchCount === 0 ? 'Explorer l\'espace' : messages[Math.min(searchCount-1, messages.length-1)]"></span>
                        <div class="absolute inset-0 bg-white/20 transform -skew-x-12 -translate-x-full group-hover:translate-x-full transition-transform duration-700"></div>
                    </button>
                </div>

                <!-- Action Buttons -->
                <div class="flex flex-col sm:flex-row gap-4 justify-center pt-6">
                    <a href="/" class="inline-flex items-center justify-center px-6 py-3 rounded-lg bg-gradient-to-r from-blue-600 to-blue-700 text-white hover:from-blue-700 hover:to-blue-800 transform hover:scale-105 transition-all duration-200">
                        <span class="mr-2">🏠</span> Retour à la base
                    </a>
                    <button onclick="history.back()" class="inline-flex items-center justify-center px-6 py-3 rounded-lg bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-600 transform hover:scale-105 transition-all duration-200">
                        <span class="mr-2">⬅️</span> Mission précédente
                    </button>
                </div>
            </div>

            <!-- Footer -->
            <p class="mt-8 text-sm text-gray-500 dark:text-gray-400">
                Besoin d'assistance terrestre ? 
                <a href="/contact" class="text-blue-600 dark:text-blue-400 hover:underline">
                    Contactez le centre de contrôle
                </a>
            </p>
        </div>
    </div>

    <script>
        // Particle Animation
        function createParticle() {
            const particle = document.createElement('div');
            particle.className = 'absolute w-1 h-1 rounded-full';
            particle.style.backgroundColor = `hsla(${Math.random() * 360}, 70%, 70%, 0.2)`;
            particle.style.left = Math.random() * 100 + 'vw';
            particle.style.top = -10 + 'px';
            document.getElementById('particles').appendChild(particle);

            gsap.to(particle, {
                y: '100vh',
                duration: Math.random() * 5 + 3,
                ease: 'none',
                onComplete: () => particle.remove()
            });
        }

        // Create particles periodically
        setInterval(createParticle, 200);

        // Initial animation
        gsap.from('.space-y-8', {
            y: 50,
            opacity: 0,
            duration: 1,
            ease: 'power3.out'
        });
    </script>

    <style>
        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }

        @media (prefers-reduced-motion: reduce) {
            .animate-float {
                animation: none;
            }
        }
    </style>
</body>
</html>